package wget;
import In.java;
/**
 * @author https://github.com/gombost84
 * Reads in a URL specified on the command line and saves its contents
 * in a file with the given name.
 */
public class Wget {
    
    public static void main(String[] args) {
        String url = args[0];
        In in = new System.In(url);
        
    }
    
}
